# Application
NODE_ENV=development
PORT=3001
API_PREFIX=api

# CORS - URL(s) do frontend autorizadas a consumir a API (separadas por vírgula)
CORS_ORIGIN=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/schedule_platform?schema=public"
